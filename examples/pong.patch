diff -ru pong/PongGame.jack pong-patched/PongGame.jack
--- pong/PongGame.jack	2016-08-29 12:22:06.000000000 +0300
+++ pong-patched/PongGame.jack	2026-04-24 15:46:10.126464775 +0300
@@ -8,7 +8,7 @@
  */
 class PongGame {
 
-    static PongGame instance; // the singelton, a Pong game instance     
+    static PongGame instance; // the singelton, a Pong game instance
     field Bat bat;            // the bat
     field Ball ball;          // the ball
     field int wall;           // the current wall that the ball is bouncing off of.
@@ -21,7 +21,6 @@
 
     /** Constructs a new Pong game. */
     constructor PongGame new() {
-	    do Screen.clearScreen();
         let batWidth = 50;  // initial bat size
         let bat = Bat.new(230, 229, batWidth, 7);
         let ball = Ball.new(253, 222, 0, 511, 0, 229);
@@ -29,7 +28,7 @@
         do Screen.drawRectangle(0, 238, 511, 240);
 	    do Output.moveCursor(22,0);
 	    do Output.printString("Score: 0");
-	
+
 	    let exit = false;
 	    let score = 0;
 	    let wall = 0;
@@ -51,7 +50,7 @@
         let instance = PongGame.new();
         return;
     }
-    
+
     /** Returns the single instance of this Pong game. */
     function PongGame getInstance() {
         return instance;
@@ -71,11 +70,11 @@
                 do Sys.wait(50);
             }
 
-            if (key = 130) { do bat.setDirection(1); }
+            if (key = 97) { do bat.setDirection(1); }
 	        else {
-	            if (key = 132) { do bat.setDirection(2); }
+	            if (key = 100) { do bat.setDirection(2); }
 		        else {
-                    if (key = 140) { let exit = true; }
+                    if (key = 27) { let exit = true; }
 		        }
             }
 
@@ -92,7 +91,7 @@
             do Output.moveCursor(10,27);
 	        do Output.printString("Game Over");
 	    }
-            
+
         return;
     }
 
@@ -100,7 +99,7 @@
      * Handles ball movement, including bouncing.
      * If the ball bounces off a wall, finds its new direction.
      * If the ball bounces off the bat, increases the score by one
-     * and shrinks the bat's size, to make the game more challenging. 
+     * and shrinks the bat's size, to make the game more challenging.
      */
     method void moveBall() {
         var int bouncingDirection, batLeft, batRight, ballLeft, ballRight;
@@ -114,7 +113,7 @@
             let batRight = bat.getRight();
             let ballLeft = ball.getLeft();
             let ballRight = ball.getRight();
-  
+
             if (wall = 4) {
                 let exit = (batLeft > ballRight) | (batRight < ballLeft);
                 if (~exit) {
@@ -124,7 +123,7 @@
                     }
 
                     let batWidth = batWidth - 2;
-                    do bat.setWidth(batWidth);      
+                    do bat.setWidth(batWidth);
                     let score = score + 1;
                     do Output.moveCursor(22,7);
                     do Output.printInt(score);
@@ -134,4 +133,4 @@
         }
         return;
     }
-}
\ No newline at end of file
+}
