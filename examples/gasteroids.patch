diff -ru GASteroids/DrawPoly.jack GASteroids-patched/DrawPoly.jack
--- GASteroids/DrawPoly.jack	2026-04-25 20:08:24.425896441 +0300
+++ GASteroids-patched/DrawPoly.jack	2026-04-25 19:59:05.176430822 +0300
@@ -240,7 +240,7 @@
      */
     method void erase() {
         // Erase previous poly.
-        do Screen.setColor(true);
+        do Screen.setColor(false);
         do draw(prevTX, prevTY, numVertices, true);
         return;
     }
@@ -251,11 +251,11 @@
      */
     method void redraw() {
         // Erase previous poly.
-        do Screen.setColor(true);
+        do Screen.setColor(false);
         do draw(prevTX, prevTY, numVertices, true);
 
         // Draw poly.
-        do Screen.setColor(false);
+        do Screen.setColor(true);
         do draw(currTX, currTY, numVertices, true);
 
         return;
@@ -272,7 +272,7 @@
      *          3 - Consider poly to be a set of independent, unjoined points.
      *
      */
-    method void draw(Array vX, Array vY, int vNum, bool closed) {
+    method void draw(Array vX, Array vY, int vNum, boolean closed) {
         var int i;
         var int j;
 
diff -ru GASteroids/GASteroids.jack GASteroids-patched/GASteroids.jack
--- GASteroids/GASteroids.jack	2026-04-25 20:08:24.425896441 +0300
+++ GASteroids-patched/GASteroids.jack	2026-04-25 20:09:19.665159648 +0300
@@ -14,9 +14,9 @@
  *
  * Keyboard controls:
  *   <space> - start game / spawn new ship / shoot weapon.
- *   <left arrow> - rotate ship counter-clockwise.
- *   <right arrow> - rotate ship clockwise.
- *   <up arrow> - fire thrusters.
+ *   <a> - rotate ship counter-clockwise.
+ *   <d> - rotate ship clockwise.
+ *   <w> - fire thrusters.
  *   <-> - decrease frame delay (speed up game).
  *   <+> - increase frame delay (slow down game).
  *
@@ -64,6 +64,7 @@
         var int i;
         var int j;
         var Array fieldXY;
+        var int mask, bit;
 
         let gameSpeed = 15;     // Default 15ms delay.
         let gameSpeedPrev = -1;
@@ -75,19 +76,14 @@
 
         let fieldXY = Constant.getFieldXY();
 
-        // Initialise playing field to black!
-        do Screen.setColor(true);
-        //do Screen.drawRectangle(fieldXY[0] / 10, fieldXY[1] / 10, fieldXY[2] / 10, fieldXY[3] / 10);
-        do Screen.drawRectangle(0, 0, 511, 255);
-
         // Draw playing field borders.
-        do Screen.setColor(false);
+        do Screen.setColor(true);
         do Screen.drawLine(fieldXY[0] / 10, fieldXY[1] / 10, fieldXY[0] / 10, fieldXY[3] / 10);
         do Screen.drawLine(fieldXY[2] / 10, fieldXY[1] / 10, fieldXY[2] / 10, fieldXY[3] / 10);
 
         // Play with poke.
         /*
-        do Screen.setColor(false);  // White
+        do Screen.setColor(true);  // White
         do Screen.drawLine(16, 250, 31, 250);
         do Memory.poke( 16384 + ((512/16) * 252) + 1,  21845);
         do Memory.poke( 16384 + ((512/16) * 254) + 1, -21846);
@@ -162,20 +158,26 @@
         let logo[62] = -1;
         let logo[63] = -1;
 
-        // Draw logo by poking 16bit words to screen memory.
-        let i = 0;
+        // Draw logo: iterate each 16-bit word, draw set bits as pixels
         let j = 0;
-        while (i < (logo_height * logo_width)) {
-            do Memory.poke( 16384 + ((512/16) * (207 + j)) + 1,  logo[i]);
-            do Memory.poke( 16384 + ((512/16) * (207 + j)) + 2,  logo[i+1]);
-            let i = i + 2;
+        do Screen.setColor(true);
+        while (j < logo_height) {
+            let mask = 1;
+            let bit = 0;
+            while (bit < 16) {
+                if ((logo[j * 2] & mask) = 0) {
+                    do Screen.drawPixel(bit + 16, 207 + j);
+                }
+                if ((logo[(j * 2) + 1] & mask) = 0) {
+                    do Screen.drawPixel(bit + 32, 207 + j);
+                }
+                let mask = mask + mask;
+                let bit = bit + 1;
+            }
             let j = j + 1;
         }
 
-        // Setup stand in lives, score, speed area.
-        do Screen.setColor(false);
-        do Screen.drawRectangle(0, 40, 64, 144);
-
+        do Screen.setColor(true);
         do Output.moveCursor(4, 0);
         do Output.printString("Lives");
 
@@ -184,6 +186,7 @@
 
         do Output.moveCursor(10, 0);
         do Output.printString("Speed");
+        do Screen.setColor(true);
 
         let blankSpace = "       ";
 
@@ -280,13 +283,13 @@
         if (key = 32) {             // Space
             do ship.fireShot();
         }
-	    if (key = 131) {            // Up
+	    if (key = 119) {            // Up
             do ship.fireThruster();
         }
-        if (key = 130) {            // Left
+        if (key = 97) {            // Left
             do ship.changeHeading(-10);
         }
-        if (key = 132) {             // Right
+        if (key = 100) {             // Right
             do ship.changeHeading(10);
         }
 
diff -ru GASteroids/PRNG.jack GASteroids-patched/PRNG.jack
--- GASteroids/PRNG.jack	2026-04-25 20:08:24.426896443 +0300
+++ GASteroids-patched/PRNG.jack	2026-04-25 19:59:05.179307327 +0300
@@ -31,7 +31,7 @@
  * along with this program.  If not, see <http://www.gnu.org/licenses/>.
  */
 class PRNG {
-    static int seed;
+    static int _seed;
     static int A;
     static int M;
     static int Q;
@@ -55,19 +55,19 @@
     function int randInt () {
         var int x_new;
 
-        let x_new = (A * MathsToo.mod(seed, Q)) - (R * (seed / Q));
+        let x_new = (A * MathsToo.mod(_seed, Q)) - (R * (_seed / Q));
 
         if (x_new > 0) {
-            let seed = x_new;
+            let _seed = x_new;
         } else {
-            let seed = x_new + M;
+            let _seed = x_new + M;
         }
 
-        return seed;
+        return _seed;
     }
 
     function void seed (int s) {
-        let seed = s;
+        let _seed = s;
         let M = 32749;  // Published known M and A integer values for PRNGs.
         let A = 219;
         let Q = M / A;
diff -ru GASteroids/Shots.jack GASteroids-patched/Shots.jack
--- GASteroids/Shots.jack	2026-04-25 20:08:24.426896443 +0300
+++ GASteroids-patched/Shots.jack	2026-04-25 19:59:05.179804139 +0300
@@ -133,7 +133,7 @@
         var int i;
 
         // Erase shots from previous tick.
-        do Screen.setColor(true);
+        do Screen.setColor(false);
         let i = 0;
         while (i < numShots) {
             if (shots[i] > 0) {
@@ -143,7 +143,7 @@
         }
 
         // Draw current shot position.
-        do Screen.setColor(false);
+        do Screen.setColor(true);
         let i = 0;
         while (i < numShots) {
             if (shots[i] > 0) {
@@ -165,7 +165,7 @@
         var int j;
         var Asteroid a;
 
-        do Screen.setColor(true);   // Better to call once than in loop.
+        do Screen.setColor(false);   // Better to call once than in loop.
                                     // Pixels are drawn to erase expired shots.
 
         // Iterate shots array, decrement shot expiry, update position.
diff -ru GASteroids/StarBurst.jack GASteroids-patched/StarBurst.jack
--- GASteroids/StarBurst.jack	2026-04-25 20:08:24.426896443 +0300
+++ GASteroids-patched/StarBurst.jack	2026-04-25 19:59:05.180554920 +0300
@@ -88,7 +88,7 @@
         if (r1) {
             // Erase diagonal particles.
             let prevR = r1 - sizeStep;
-            do Screen.setColor(true);
+            do Screen.setColor(false);
             do Screen.drawPixel( x + prevR, y + prevR);
             do Screen.drawPixel( x + prevR, y - prevR);
             do Screen.drawPixel( x - prevR, y - prevR);
@@ -96,7 +96,7 @@
 
             // Draw diagonal particles.
             if (r1 < r) {
-                do Screen.setColor(false);
+                do Screen.setColor(true);
                 do Screen.drawPixel( x + r1,    y + r1);
                 do Screen.drawPixel( x + r1,    y - r1);
                 do Screen.drawPixel( x - r1,    y - r1);
@@ -107,7 +107,7 @@
         if (r2) {
             // Erase vertical/horizontal particles.
             let prevR = r2 - sizeStep;
-            do Screen.setColor(true);
+            do Screen.setColor(false);
             do Screen.drawPixel( x,         y + prevR);
             do Screen.drawPixel( x + prevR, y        );
             do Screen.drawPixel( x,         y - prevR);
@@ -115,7 +115,7 @@
 
             // Draw vertical/horizontal particles.
             if (r2 < r) {
-                do Screen.setColor(false);
+                do Screen.setColor(true);
                 do Screen.drawPixel( x,         y + r2);
                 do Screen.drawPixel( x + r2,    y     );
                 do Screen.drawPixel( x,         y - r2);
